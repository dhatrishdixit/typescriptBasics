let tUser: [string,number,boolean] ;

tUser = ['hc',131,true] ; 
//tUser.push(true) 
// this is a problem in tuples it doesnt recognize these type of errors , that is you can use all types of push , etc array methods 

type sampleType = [string,number,boolean];
let sampleArray : sampleType = [
    'dhatrish',
    22,
    true
]
